server:
  port: 8763

spring:
  application:
    name: mbti-service-user
  cloud:
    config:
      enabled: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mbti-app-db?useSSL=false&serverTimezone=UTC
    sername: root
    password:
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

